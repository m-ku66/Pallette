import React from "react";
import AudioController from "../ui/AudioController";

const GameOverScreen = () => {
  return (
    <div>
      <AudioController /> game over
    </div>
  );
};

export default GameOverScreen;
